import { ApearAnimationDirective } from './apear-animation.directive';

/* describe('ApearAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ApearAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
 */